import ODCInterfaceException from "./ODCInterfaceException.mjs";
import EventEmitter from "events";

class ODCServiceInterface extends EventEmitter{
    constructor(){
        super();
        // if(!this.getServiceType)
        //     throw new ODCInterfaceException("getServiceType() must be implemented");
        // if(!this.getState)
        //     throw new ODCInterfaceException("getState() must be implemented");
        // if(!this.sendCommand)
        //     throw new ODCInterfaceException("sendCommand(cmd) must be implemented");
        // if(!this.connect)
        //     throw new ODCInterfaceException("connect() must be implemented");
        // if(!this.disconnect)
        //     throw new ODCInterfaceException("disconnect() must be implemented");
        // if(!this.isConnected)
        //     throw new ODCInterfaceException("isConnected() must be implemented");
        // if(!this.help)
        //     throw new ODCInterfaceException("help() must be implemented");
        // if(!this.update)
        //     throw new ODCInterfaceException("update() must be implemented");
        // if(!this.run)
        //     throw new ODCInterfaceException("run() must be implemented");
    }
}
export default ODCServiceInterface;