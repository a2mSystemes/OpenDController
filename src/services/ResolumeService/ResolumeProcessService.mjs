

export default class ResolumeProcessService{
    constructor(config){ init(config); }

    init(){
        
    }
    start(){}
    stop(pid = undefined){}
    restart(project){}
    isRunning(){}
}
